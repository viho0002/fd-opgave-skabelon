---

---

<header>
  <div>
    <h2>AskExperts</h2>
    <nav>
      <a href="">Home</a>
      <a href="">Case Study</a>
      <a href="">Team</a>
      <a href="">About</a>
    </nav>
    <button data-button="yellow" popovertarget="my-pop">Login</button>
    <div id="my-pop" popover>
      <h2>Dashboard Login</h2>
      <p>Email adress</p>
      <input type="text" name="" id="" />
      <p>Password</p>
      <input type="text" name="" id="" />
      <button data-button="yellow">Go to dashboard</button>
    </div>
  </div>
</header>

<style>
  header {
    background-color: var(--prim-color-3);
    color: var(--sec-color-1);
    padding-block: 20px;

    div {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;

      nav {
        display: flex;
        gap: 1rem;
        a {
          text-decoration: none;
          color: var(--sec-color-1);
        }
      }
      button {
        justify-self: end;
      }
    }
  }

  /* POPOVER  */
  #my-pop {
    background-color: var(--prim-color-3);
    color: var(--sec-color-2);
    display: flex;
    flex-direction: column;
    padding-inline: 120px;
    padding-block: 40px;
    gap: 5px;
    border-radius: 20px;
    p {
      place-self: start;
    }
    input {
      background-color: var(--sec-color-2);
    }
  }

  [popover] {
    &,
    &::backdrop {
      transition:
        display 0.5s allow-discrete,
        overlay 0.5s allow-discrete,
        opacity 0.5s;
      opacity: 0;
    }

    &::backdrop {
      background: black;
    }

    &:popover-open {
      opacity: 1;

      &::backdrop {
        opacity: 0.5;
      }
    }

    @starting-style {
      &:popover-open,
      &:popover-open::backdrop {
        opacity: 0;
      }
    }
  }
  /* POPOVER  */

  /* Styles her */
</style>
